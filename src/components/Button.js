import { StyledButton } from "./styles/Button.styled";

function Button () {
    return (
        <StyledButton footer>
            <p>Try It Free</p>
        </StyledButton>
    )
}

export default Button